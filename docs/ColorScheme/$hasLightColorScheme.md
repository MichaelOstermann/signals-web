# $hasLightColorScheme

```ts
const $hasLightColorScheme: Signal<boolean>;
```

A signal indicating whether the user prefers a light color scheme.

## Example

```ts
import { $hasLightColorScheme } from "@monstermann/signals-web";

$hasLightColorScheme(); // true
```
