# $hasReducedMotionPreference

```ts
const $hasReducedMotionPreference: Signal<boolean>;
```

A signal indicating whether the user prefers reduced motion.

## Example

```ts
import { $hasReducedMotionPreference } from "@monstermann/signals-web";

$hasReducedMotionPreference(); // true
```
