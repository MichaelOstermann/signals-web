# $pointerX

```ts
const $pointerX: Signal<number>;
```

A signal containing the current pointer x coordinate.

## Example

```ts
import { $pointerX } from "@monstermann/signals-web";

$pointerX(); // 450
```
